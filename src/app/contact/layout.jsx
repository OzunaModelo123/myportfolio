export const metadata = {
  title: 'Contact',
  description: 'Get in touch with Daniel Ozoani for project collaborations, technical management, or consulting.',
};

export default function ContactLayout({ children }) {
  return children;
}
