export const metadata = {
  title: 'Projects',
  description: 'A collection of my recent projects, from B2B SaaS to Social Wellness apps.',
};

export default function ProjectsLayout({ children }) {
  return children;
}
